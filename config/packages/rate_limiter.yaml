framework:
    rate_limiter:
        login:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
            cache_pool: 'cache.app'
        api_general:
            policy: 'sliding_window'
            limit: 120
            interval: '1 minute'
            cache_pool: 'cache.app'
        campaign_send:
            policy: 'fixed_window'
            limit: 10
            interval: '1 hour'
            cache_pool: 'cache.app'
